doctype html
html(lang="zh-CN")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title Poleis - 远程控制管理平台
    link(rel="preconnect" href="https://fonts.googleapis.com")
    link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin="")
    link(href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700;900&family=Noto+Sans+SC:wght@400;500;700&display=swap" rel="stylesheet")
    link(rel="stylesheet" href="/stylesheets/material_icons/material_icons.css")
    style.
      * { box-sizing: border-box; }
      html { scroll-behavior: smooth; }
      html, body { height: 100%; margin: 0; font-family: 'Roboto','Noto Sans SC',sans-serif; }
      #root { width: 100%; min-height: 100%; }
  body
    div#root
    script(src="/vendor/react.js")
    script(src="/vendor/react-dom.js")
    script(src="/vendor/material-ui.js")
    script(src="/vendor/emotion-react.js")
    script(src="/vendor/emotion-styled.js")
    script.
      const { useState, useEffect, useMemo } = React;
      const { createRoot } = ReactDOM;
      const {
        ThemeProvider,
        createTheme,
        CssBaseline,
        Box,
        Container,
        Typography,
        Button,
        Stack,
        Paper,
        Grid,
        AppBar,
        Toolbar,
        IconButton,
        Drawer,
        List,
        ListItemButton,
        ListItemText,
        ListItemIcon,
        Divider,
        Chip,
        Icon,
        Fade,
        Grow,
        Slide,
        Avatar,
        Card,
        CardContent,
        useScrollTrigger,
        LinearProgress
      } = MaterialUI;

      // 主题配置
      const landingTheme = createTheme({
        palette: {
          mode: 'light',
          primary: { main: '#3040d6', light: '#5c6bc0', dark: '#1a237e' },
          secondary: { main: '#7c3aed', light: '#b388ff', dark: '#4a148c' },
          background: { default: '#fafbfc', paper: '#ffffff' },
          text: { primary: '#1a1a2e', secondary: '#4a5568' }
        },
        typography: {
          fontFamily: "'Roboto','Noto Sans SC',sans-serif",
          h1: { fontWeight: 800, letterSpacing: '-0.02em' },
          h2: { fontWeight: 700, letterSpacing: '-0.01em' },
          h3: { fontWeight: 600 },
          h4: { fontWeight: 600 },
          h5: { fontWeight: 500 },
          h6: { fontWeight: 500 }
        },
        shape: { borderRadius: 12 },
        components: {
          MuiButton: {
            styleOverrides: {
              root: { textTransform: 'none', fontWeight: 600, borderRadius: 8 }
            }
          }
        }
      });

      // 动画数字计数器组件
      const AnimatedCounter = ({ end, duration = 2000, suffix = '' }) => {
        const [count, setCount] = useState(0);
        const [started, setStarted] = useState(false);

        useEffect(() => {
          if (!started) return;
          let startTime;
          const step = (timestamp) => {
            if (!startTime) startTime = timestamp;
            const progress = Math.min((timestamp - startTime) / duration, 1);
            setCount(Math.floor(progress * end));
            if (progress < 1) requestAnimationFrame(step);
          };
          requestAnimationFrame(step);
        }, [started, end, duration]);

        useEffect(() => {
          const timer = setTimeout(() => setStarted(true), 500);
          return () => clearTimeout(timer);
        }, []);

        return React.createElement(Typography, {
          variant: 'h3',
          fontWeight: 700,
          color: 'primary.main'
        }, count.toLocaleString() + suffix);
      };

      // 特性卡片组件
      const FeatureCard = ({ icon, title, description, delay = 0 }) => {
        const [visible, setVisible] = useState(false);
        useEffect(() => {
          const timer = setTimeout(() => setVisible(true), delay);
          return () => clearTimeout(timer);
        }, []);

        return React.createElement(Grow, { in: visible, timeout: 600 },
          React.createElement(Card, {
            elevation: 0,
            sx: {
              height: '100%',
              borderRadius: 3,
              border: '1px solid',
              borderColor: 'divider',
              transition: 'all 0.3s ease',
              '&:hover': {
                borderColor: 'primary.main',
                boxShadow: '0 8px 32px rgba(48,64,214,0.12)',
                transform: 'translateY(-4px)'
              }
            }
          },
            React.createElement(CardContent, { sx: { p: 3 } },
              React.createElement(Box, {
                sx: {
                  width: 56,
                  height: 56,
                  borderRadius: 2,
                  bgcolor: 'primary.main',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  mb: 2,
                  color: '#fff'
                }
              },
                React.createElement(Icon, { sx: { fontSize: 28 } }, icon)
              ),
              React.createElement(Typography, { variant: 'h6', gutterBottom: true, fontWeight: 600 }, title),
              React.createElement(Typography, { variant: 'body2', color: 'text.secondary', lineHeight: 1.7 }, description)
            )
          )
        );
      };

      // 导航栏组件
      const NavBar = () => {
        const [scrolled, setScrolled] = useState(false);
        const [mobileOpen, setMobileOpen] = useState(false);

        useEffect(() => {
          const handleScroll = () => setScrolled(window.scrollY > 50);
          window.addEventListener('scroll', handleScroll);
          return () => window.removeEventListener('scroll', handleScroll);
        }, []);

        const navItems = [
          { label: '功能特性', href: '#features' },
          { label: '系统架构', href: '#architecture' },
          { label: '应用场景', href: '#usecases' },
          { label: '关于我们', href: '#about' }
        ];

        const drawerContent = React.createElement(Box, { sx: { width: 250 } },
          React.createElement(Box, { sx: { p: 2 } },
            React.createElement(Box, {
              sx: {
                position: 'relative',
                display: 'inline-flex',
                alignItems: 'baseline'
              }
            },
              React.createElement(Typography, { variant: 'h6', fontWeight: 700, color: 'primary.main' }, 'Poleis'),
              React.createElement(Box, {
                component: 'span',
                sx: {
                  display: 'inline-flex',
                  alignSelf: 'flex-start',
                  ml: 0.75,
                  mt: 0.25,
                  fontSize: '0.6rem',
                  fontWeight: 700,
                  lineHeight: 1.2,
                  px: 0.6,
                  py: 0.15,
                  borderRadius: 1,
                  bgcolor: 'secondary.main',
                  color: '#fff',
                  textTransform: 'uppercase',
                  letterSpacing: '0.05em'
                }
              }, 'beta')
            )
          ),
          React.createElement(Divider, null),
          React.createElement(List, null,
            navItems.map((item) =>
              React.createElement(ListItemButton, {
                key: item.href,
                component: 'a',
                href: item.href,
                onClick: () => setMobileOpen(false)
              },
                React.createElement(ListItemText, { primary: item.label })
              )
            )
          )
        );

        return React.createElement(AppBar, {
          position: 'fixed',
          elevation: scrolled ? 4 : 0,
          sx: {
            bgcolor: scrolled ? 'rgba(255,255,255,0.95)' : 'transparent',
            backdropFilter: scrolled ? 'blur(8px)' : 'none',
            borderBottom: scrolled ? '1px solid' : 'none',
            borderColor: 'divider',
            transition: 'all 0.3s ease'
          }
        },
          React.createElement(Toolbar, { sx: { py: 1 } },
            React.createElement(Box, {
              component: 'a',
              href: '/',
              sx: {
                position: 'relative',
                display: 'inline-flex',
                alignItems: 'baseline',
                flexGrow: { xs: 1, md: 0 },
                fontWeight: 800,
                color: scrolled ? 'primary.main' : '#fff',
                textDecoration: 'none',
                letterSpacing: '-0.02em',
                fontSize: '1.5rem'
              }
            },
              'Poleis',
              React.createElement(Box, {
                component: 'span',
                sx: {
                  display: 'inline-flex',
                  alignSelf: 'flex-start',
                  ml: 0.75,
                  mt: 0.25,
                  fontSize: '0.6rem',
                  fontWeight: 700,
                  lineHeight: 1.2,
                  px: 0.6,
                  py: 0.15,
                  borderRadius: 1,
                  bgcolor: 'secondary.main',
                  color: '#fff',
                  textTransform: 'uppercase',
                  letterSpacing: '0.05em'
                }
              }, 'beta')
            ),
            React.createElement(Box, {
              sx: {
                display: { xs: 'none', md: 'flex' },
                flexGrow: 1,
                gap: 4,
                ml: 6
              }
            },
              navItems.map((item) =>
                React.createElement(Typography, {
                  key: item.href,
                  component: 'a',
                  href: item.href,
                  sx: {
                    color: scrolled ? 'text.primary' : 'rgba(255,255,255,0.85)',
                    textDecoration: 'none',
                    fontWeight: 500,
                    fontSize: '0.95rem',
                    transition: 'color 0.2s',
                    '&:hover': { color: scrolled ? 'primary.main' : '#fff' }
                  }
                }, item.label)
              )
            ),
            React.createElement(Box, { sx: { display: { xs: 'none', md: 'flex' }, gap: 2 } },
              React.createElement(Button, {
                variant: 'text',
                href: '/login',
                sx: { color: scrolled ? 'text.primary' : '#fff' }
              }, '登录'),
              React.createElement(Button, {
                variant: 'contained',
                href: '/admin',
                disableElevation: true,
                sx: {
                  bgcolor: scrolled ? 'primary.main' : '#fff',
                  color: scrolled ? '#fff' : 'primary.main',
                  '&:hover': {
                    bgcolor: scrolled ? 'primary.dark' : 'rgba(255,255,255,0.9)'
                  }
                }
              }, '进入后台')
            ),
            React.createElement(IconButton, {
              sx: { display: { md: 'none' }, color: scrolled ? 'primary.main' : '#fff' },
              onClick: () => setMobileOpen(true)
            },
              React.createElement(Icon, null, 'menu')
            )
          ),
          React.createElement(Drawer, {
            anchor: 'right',
            open: mobileOpen,
            onClose: () => setMobileOpen(false)
          }, drawerContent)
        );
      };

      // Hero 区域
      const HeroSection = () => {
        const [loaded, setLoaded] = useState(false);
        useEffect(() => {
          setLoaded(true);
        }, []);

        return React.createElement(Box, {
          sx: {
            minHeight: '100vh',
            background: 'linear-gradient(135deg, #1a237e 0%, #3040d6 50%, #7c3aed 100%)',
            position: 'relative',
            overflow: 'hidden',
            display: 'flex',
            alignItems: 'center'
          }
        },
          // 背景装饰
          React.createElement(Box, {
            sx: {
              position: 'absolute',
              top: 0,
              left: 0,
              right: 0,
              bottom: 0,
              opacity: 0.1,
              background: `url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E")`
            }
          }),
          React.createElement(Box, {
            sx: {
              position: 'absolute',
              top: '10%',
              right: '5%',
              width: 400,
              height: 400,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(124,58,237,0.3) 0%, transparent 70%)',
              filter: 'blur(40px)'
            }
          }),
          React.createElement(Box, {
            sx: {
              position: 'absolute',
              bottom: '20%',
              left: '10%',
              width: 300,
              height: 300,
              borderRadius: '50%',
              background: 'radial-gradient(circle, rgba(48,64,214,0.4) 0%, transparent 70%)',
              filter: 'blur(40px)'
            }
          }),
          // 内容
          React.createElement(Container, { maxWidth: 'lg', sx: { position: 'relative', zIndex: 1, pt: 12 } },
            React.createElement(Grid, { container: true, spacing: 6, alignItems: 'center' },
              React.createElement(Grid, { item: true, xs: 12, md: 7 },
                React.createElement(Fade, { in: loaded, timeout: 800 },
                  React.createElement(Box, null,
                    React.createElement(Chip, {
                      label: '高性能远程桌面软件',
                      sx: {
                        mb: 3,
                        bgcolor: 'rgba(255,255,255,0.15)',
                        color: '#fff',
                        fontWeight: 500,
                        border: '1px solid rgba(255,255,255,0.2)'
                      }
                    }),
                    React.createElement(Typography, {
                      variant: 'h1',
                      sx: {
                        color: '#fff',
                        fontSize: { xs: '2.5rem', md: '3.5rem', lg: '4rem' },
                        lineHeight: 1.2,
                        mb: 3
                      }
                    }, '低延迟远程桌面，', React.createElement('br', null),
                      React.createElement('span', { style: { color: '#a5b4fc' } }, '随时随地控制你的电脑')
                    ),
                    React.createElement(Typography, {
                      variant: 'h6',
                      sx: {
                        color: 'rgba(255,255,255,0.8)',
                        fontWeight: 400,
                        maxWidth: 600,
                        mb: 4,
                        lineHeight: 1.7
                      }
                    }, 'Poleis 是一款高性能远程桌面软件。基于硬件编码加速与智能 P2P 穿透技术，让你在任何地方都能获得如本地般的流畅操控体验。'),
                    React.createElement(Stack, {
                      direction: { xs: 'column', sm: 'row' },
                      spacing: 2,
                      sx: { flexWrap: 'wrap' }
                    },
                      React.createElement(Button, {
                        variant: 'contained',
                        size: 'large',
                        href: '/admin',
                        sx: {
                          bgcolor: '#fff',
                          color: 'primary.main',
                          px: 4,
                          py: 1.5,
                          fontSize: '1rem',
                          '&:hover': { bgcolor: 'rgba(255,255,255,0.9)' }
                        },
                        endIcon: React.createElement(Icon, null, 'arrow_forward')
                      }, '立即开始'),
                      React.createElement(Button, {
                        variant: 'outlined',
                        size: 'large',
                        href: '#features',
                        sx: {
                          borderColor: 'rgba(255,255,255,0.5)',
                          color: '#fff',
                          px: 4,
                          py: 1.5,
                          fontSize: '1rem',
                          '&:hover': {
                            borderColor: '#fff',
                            bgcolor: 'rgba(255,255,255,0.1)'
                          }
                        }
                      }, '了解更多')
                    )
                  )
                )
              ),
              React.createElement(Grid, {
                item: true,
                xs: 12,
                md: 5,
                sx: { display: { xs: 'none', md: 'block' } }
              },
                React.createElement(Slide, { in: loaded, direction: 'left', timeout: 1000 },
                  React.createElement(Paper, {
                    elevation: 24,
                    sx: {
                      borderRadius: 4,
                      overflow: 'hidden',
                      background: 'linear-gradient(145deg, rgba(255,255,255,0.15) 0%, rgba(255,255,255,0.05) 100%)',
                      backdropFilter: 'blur(10px)',
                      border: '1px solid rgba(255,255,255,0.2)',
                      p: 3
                    }
                  },
                    React.createElement(Box, {
                      sx: {
                        bgcolor: 'rgba(0,0,0,0.3)',
                        borderRadius: 2,
                        p: 2,
                        mb: 2
                      }
                    },
                      React.createElement(Typography, { variant: 'caption', color: 'rgba(255,255,255,0.6)' }, '核心技术'),
                      React.createElement(Box, { sx: { mt: 1.5, display: 'flex', flexWrap: 'wrap', gap: 1 } },
                        React.createElement(Chip, { label: 'H.264 / H.265', size: 'small', sx: { bgcolor: 'rgba(74,222,128,0.2)', color: '#4ade80', border: '1px solid rgba(74,222,128,0.3)' } }),
                        React.createElement(Chip, { label: 'NVENC / AMF 硬件编码', size: 'small', sx: { bgcolor: 'rgba(96,165,250,0.2)', color: '#60a5fa', border: '1px solid rgba(96,165,250,0.3)' } }),
                        React.createElement(Chip, { label: 'QUIC 加密传输', size: 'small', sx: { bgcolor: 'rgba(251,191,36,0.2)', color: '#fbbf24', border: '1px solid rgba(251,191,36,0.3)' } }),
                        React.createElement(Chip, { label: 'P2P 智能穿透', size: 'small', sx: { bgcolor: 'rgba(167,139,250,0.2)', color: '#a78bfa', border: '1px solid rgba(167,139,250,0.3)' } }),
                        React.createElement(Chip, { label: '60FPS+ 高帧率', size: 'small', sx: { bgcolor: 'rgba(244,114,182,0.2)', color: '#f472b6', border: '1px solid rgba(244,114,182,0.3)' } }),
                        React.createElement(Chip, { label: '4:4:4 真彩色', size: 'small', sx: { bgcolor: 'rgba(52,211,153,0.2)', color: '#34d399', border: '1px solid rgba(52,211,153,0.3)' } })
                      )
                    ),
                    React.createElement(Stack, { direction: 'row', spacing: 1, justifyContent: 'center' },
                      React.createElement(Chip, {
                        label: 'Windows 控制端',
                        size: 'small',
                        sx: { bgcolor: 'rgba(255,255,255,0.1)', color: '#fff' }
                      }),
                      React.createElement(Chip, {
                        label: 'Windows 被控端',
                        size: 'small',
                        sx: { bgcolor: 'rgba(255,255,255,0.1)', color: '#fff' }
                      }),
                      React.createElement(Chip, {
                        label: 'Web 管理后台',
                        size: 'small',
                        sx: { bgcolor: 'rgba(255,255,255,0.1)', color: '#fff' }
                      })
                    )
                  )
                )
              )
            )
          )
        );
      };

      // 功能特性区域
      const FeaturesSection = () => {
        const features = [
          {
            icon: 'bolt',
            title: '超低延迟远程桌面',
            description: 'NVENC/AMF 硬件编码加速，H.264/H.265 视频压缩，支持 60FPS+ 高帧率与 4:4:4 真彩色，操控响应如本地般流畅。'
          },
          {
            icon: 'language',
            title: '智能 P2P 穿透',
            description: '多 STUN 服务器 NAT 类型检测 + 多策略端口预测打洞，QUIC 加密传输，复杂网络环境下自动中继保底。'
          },
          {
            icon: 'screenshot_monitor',
            title: '多显示器支持',
            description: '每显示器独立视频流通道，按需切换或合屏显示，支持虚拟显示器适配无头主机，动态分辨率跟随。'
          },
          {
            icon: 'support_agent',
            title: '远程协助',
            description: '伙伴 ID + 6 位密码一键连接，无需注册账号即可接受或提供临时技术支持，类似 TeamViewer 使用体验。'
          },
          {
            icon: 'drive_folder_upload',
            title: '文件传输与剪贴板',
            description: '拖拽传文件、剪贴板双向同步（文本/图片/文件），高效便捷，让远程工作协作无缝衔接。'
          },
          {
            icon: 'sports_esports',
            title: '外设支持',
            description: '游戏手柄远程操控（SDL2 采集 + ViGEm 虚拟手柄），支持 Xbox/PS/Switch Pro 控制器，含体感与震动反馈。'
          }
        ];

        return React.createElement(Box, {
          id: 'features',
          sx: { py: { xs: 8, md: 12 }, bgcolor: 'background.default' }
        },
          React.createElement(Container, { maxWidth: 'lg' },
            React.createElement(Box, { sx: { textAlign: 'center', mb: 8 } },
              React.createElement(Typography, {
                variant: 'overline',
                color: 'primary.main',
                sx: { fontWeight: 600, letterSpacing: 2 }
              }, 'FEATURES'),
              React.createElement(Typography, {
                variant: 'h2',
                sx: { mt: 1, color: 'text.primary' }
              }, '核心功能'),
              React.createElement(Typography, {
                variant: 'h6',
                color: 'text.secondary',
                sx: { mt: 2, maxWidth: 600, mx: 'auto', fontWeight: 400 }
              }, 'Poleis 提供完整的远程控制能力，覆盖从远程办公到 IT 运维的全场景需求')
            ),
            React.createElement(Grid, { container: true, spacing: 3 },
              features.map((feature, index) =>
                React.createElement(Grid, { item: true, xs: 12, sm: 6, md: 4, key: index },
                  React.createElement(FeatureCard, {
                    icon: feature.icon,
                    title: feature.title,
                    description: feature.description,
                    delay: index * 100
                  })
                )
              )
            )
          )
        );
      };

      // 系统架构区域
      const ArchitectureSection = () => {
        const components = [
          { name: '控制端 (Client)', desc: 'Qt 6 桌面应用，D3D11 零拷贝渲染，SDL2 输入采集，低延迟解码回放', color: '#3040d6', icon: 'computer' },
          { name: '信令服务 (Server)', desc: 'Socket.io 实时信令调度，UDP 中继服务保底，企业设备管理与审计', color: '#7c3aed', icon: 'cloud' },
          { name: '被控端 (Agent)', desc: 'DXGI 桌面采集，NVENC/AMF 硬件编码，ViGEm 外设注入，Windows 服务运行', color: '#059669', icon: 'memory' }
        ];

        return React.createElement(Box, {
          id: 'architecture',
          sx: { py: { xs: 8, md: 12 }, bgcolor: 'grey.50' }
        },
          React.createElement(Container, { maxWidth: 'lg' },
            React.createElement(Box, { sx: { textAlign: 'center', mb: 8 } },
              React.createElement(Typography, {
                variant: 'overline',
                color: 'primary.main',
                sx: { fontWeight: 600, letterSpacing: 2 }
              }, 'ARCHITECTURE'),
              React.createElement(Typography, {
                variant: 'h2',
                sx: { mt: 1 }
              }, '系统架构'),
              React.createElement(Typography, {
                variant: 'h6',
                color: 'text.secondary',
                sx: { mt: 2, maxWidth: 600, mx: 'auto', fontWeight: 400 }
              }, '控制端与被控端通过信令服务建立 P2P 直连，硬件编码后经 QUIC 通道实时传输')
            ),
            React.createElement(Box, {
              sx: {
                display: 'flex',
                flexDirection: { xs: 'column', md: 'row' },
                justifyContent: 'center',
                alignItems: 'center',
                gap: 4,
                flexWrap: 'wrap'
              }
            },
              components.map((comp, index) =>
                React.createElement(Fade, { in: true, key: index, timeout: 800 + index * 200 },
                  React.createElement(Paper, {
                    elevation: 8,
                    sx: {
                      width: { xs: '100%', md: 280 },
                      p: 4,
                      borderRadius: 4,
                      textAlign: 'center',
                      position: 'relative',
                      overflow: 'hidden',
                      '&::before': {
                        content: '""',
                        position: 'absolute',
                        top: 0,
                        left: 0,
                        right: 0,
                        height: 4,
                        bgcolor: comp.color
                      }
                    }
                  },
                    React.createElement(Box, {
                      sx: {
                        width: 72,
                        height: 72,
                        borderRadius: '50%',
                        bgcolor: comp.color,
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                        mx: 'auto',
                        mb: 2,
                        color: '#fff'
                      }
                    },
                      React.createElement(Icon, { sx: { fontSize: 36 } }, comp.icon)
                    ),
                    React.createElement(Typography, { variant: 'h5', fontWeight: 600 }, comp.name),
                    React.createElement(Typography, { variant: 'body2', color: 'text.secondary', sx: { mt: 1 } }, comp.desc)
                  )
                )
              ),
              // 连接线
              React.createElement(Box, {
                sx: {
                  display: { xs: 'none', md: 'flex' },
                  position: 'absolute',
                  width: '60%',
                  maxWidth: 800,
                  justifyContent: 'space-around',
                  alignItems: 'center',
                  mt: 15,
                  pointerEvents: 'none'
                }
              })
            ),
            // 技术栈
            React.createElement(Box, { sx: { mt: 8, textAlign: 'center' } },
              React.createElement(Typography, { variant: 'subtitle1', color: 'text.secondary', gutterBottom: true }, '技术栈'),
              React.createElement(Stack, {
                direction: 'row',
                spacing: 2,
                justifyContent: 'center',
                flexWrap: 'wrap',
                useFlexGap: true,
                sx: { mt: 2 }
              },
                ['C++17', 'Qt 6', 'QUIC', 'NVENC', 'DXGI', 'Node.js', 'Socket.io', 'React', 'MySQL', 'Redis'].map((tech) =>
                  React.createElement(Chip, {
                    key: tech,
                    label: tech,
                    variant: 'outlined',
                    sx: { borderRadius: 2, px: 1 }
                  })
                )
              )
            )
          )
        );
      };

      // 应用场景区域
      const UseCasesSection = () => {
        const useCases = [
          {
            icon: 'home_office',
            title: '远程办公',
            description: '随时随地访问办公室电脑，低延迟高清画质，支持多显示器与文件传输，工作效率不打折。'
          },
          {
            icon: 'build',
            title: 'IT 运维',
            description: '批量管理企业设备资产，远程故障排查与系统维护，无需亲临现场即可完成日常运维。'
          },
          {
            icon: 'stadia_controller',
            title: '远程游戏',
            description: '低延迟游戏串流，支持手柄与键鼠，在外也能畅玩家中高配电脑上的 3A 大作。'
          },
          {
            icon: 'contact_support',
            title: '临时协助',
            description: '伙伴 ID + 密码快速连接，无需注册安装，适合帮朋友或家人解决电脑问题。'
          }
        ];

        return React.createElement(Box, {
          id: 'usecases',
          sx: {
            py: { xs: 8, md: 12 },
            background: 'linear-gradient(135deg, #1a237e 0%, #3040d6 100%)'
          }
        },
          React.createElement(Container, { maxWidth: 'lg' },
            React.createElement(Box, { sx: { textAlign: 'center', mb: 8 } },
              React.createElement(Typography, {
                variant: 'overline',
                sx: { color: 'rgba(255,255,255,0.6)', fontWeight: 600, letterSpacing: 2 }
              }, 'USE CASES'),
              React.createElement(Typography, {
                variant: 'h2',
                sx: { mt: 1, color: '#fff' }
              }, '应用场景'),
              React.createElement(Typography, {
                variant: 'h6',
                sx: { mt: 2, maxWidth: 600, mx: 'auto', fontWeight: 400, color: 'rgba(255,255,255,0.7)' }
              }, '覆盖个人到企业的完整远程控制需求')
            ),
            React.createElement(Grid, { container: true, spacing: 3, justifyContent: 'center' },
              useCases.map((uc, index) =>
                React.createElement(Grid, { item: true, xs: 12, sm: 6, md: 3, key: index },
                  React.createElement(Grow, { in: true, timeout: 600 + index * 150 },
                    React.createElement(Paper, {
                      elevation: 0,
                      sx: {
                        height: '100%',
                        p: 3,
                        borderRadius: 3,
                        textAlign: 'center',
                        bgcolor: 'rgba(255,255,255,0.08)',
                        backdropFilter: 'blur(8px)',
                        border: '1px solid rgba(255,255,255,0.12)',
                        transition: 'all 0.3s ease',
                        '&:hover': {
                          bgcolor: 'rgba(255,255,255,0.15)',
                          transform: 'translateY(-4px)'
                        }
                      }
                    },
                      React.createElement(Icon, {
                        sx: { fontSize: 48, color: '#a5b4fc', mb: 2 }
                      }, uc.icon),
                      React.createElement(Typography, {
                        variant: 'h6',
                        fontWeight: 600,
                        color: '#fff',
                        gutterBottom: true
                      }, uc.title),
                      React.createElement(Typography, {
                        variant: 'body2',
                        color: 'rgba(255,255,255,0.7)',
                        lineHeight: 1.7
                      }, uc.description)
                    )
                  )
                )
              )
            )
          )
        );
      };

      // 关于区域
      const AboutSection = () => {
        return React.createElement(Box, {
          id: 'about',
          sx: { py: { xs: 8, md: 12 }, bgcolor: 'background.default' }
        },
          React.createElement(Container, { maxWidth: 'md' },
            React.createElement(Box, { sx: { textAlign: 'center' } },
              React.createElement(Typography, {
                variant: 'overline',
                color: 'primary.main',
                sx: { fontWeight: 600, letterSpacing: 2 }
              }, 'ABOUT'),
              React.createElement(Typography, {
                variant: 'h2',
                sx: { mt: 1 }
              }, '关于 Poleis'),
              React.createElement(Typography, {
                variant: 'h6',
                color: 'text.secondary',
                sx: { mt: 3, fontWeight: 400, lineHeight: 1.8 }
              }, 'Poleis 是一款面向企业与个人的高性能远程桌面软件。采用硬件编码加速、QUIC 加密传输与智能 NAT 穿透技术，致力于提供安全、流畅、易用的远程控制体验。支持远程办公、IT 运维、远程游戏等多种应用场景。'),
              React.createElement(Box, { sx: { mt: 4 } },
                React.createElement(Button, {
                  variant: 'contained',
                  size: 'large',
                  href: '/admin',
                  sx: { px: 6, py: 1.5 }
                }, '立即体验')
              )
            )
          )
        );
      };

      // Footer
      const Footer = () => {
        return React.createElement(Box, {
          component: 'footer',
          sx: {
            py: 4,
            bgcolor: 'grey.900',
            color: 'grey.400'
          }
        },
          React.createElement(Container, { maxWidth: 'lg' },
            React.createElement(Stack, {
              direction: { xs: 'column', md: 'row' },
              justifyContent: 'space-between',
              alignItems: 'center',
              spacing: 2
            },
              React.createElement(Typography, { variant: 'body2' },
                '\u00A9 2026 Poleis. All rights reserved.'
              ),
              React.createElement(Stack, { direction: 'row', spacing: 3 },
                React.createElement(Typography, {
                  variant: 'body2',
                  component: 'a',
                  href: '/login',
                  sx: { color: 'grey.400', textDecoration: 'none', '&:hover': { color: '#fff' } }
                }, '登录'),
                React.createElement(Typography, {
                  variant: 'body2',
                  component: 'a',
                  href: '/admin',
                  sx: { color: 'grey.400', textDecoration: 'none', '&:hover': { color: '#fff' } }
                }, '管理后台')
              )
            )
          )
        );
      };

      // 主应用
      const LandingApp = () => {
        return React.createElement(ThemeProvider, { theme: landingTheme },
          React.createElement(CssBaseline, null),
          React.createElement(Box, { sx: { minHeight: '100vh' } },
            React.createElement(NavBar, null),
            React.createElement(HeroSection, null),
            React.createElement(FeaturesSection, null),
            React.createElement(ArchitectureSection, null),
            React.createElement(UseCasesSection, null),
            React.createElement(AboutSection, null),
            React.createElement(Footer, null)
          )
        );
      };

      const root = createRoot(document.getElementById('root'));
      root.render(React.createElement(LandingApp));
